#include <iostream>
// #include <math.h>
using namespace std;

int main()
{
    int r=0;
    cout << "Enter radius (in cm) : ";
    cin >> r;
    cout << "Area of circle = " << 3.14 * r * r << " cm sq.";
}